# Docker环境变量配置示例
# 使用方法：docker-compose --env-file env.docker up

# NodePassDash Docker 环境变量配置

# 服务端口
PORT=3000

# 日志级别 (DEBUG, INFO, WARN, ERROR)
LOG_LEVEL=INFO

# TLS 证书配置 (可选)
# TLS_CERT=/path/to/cert.pem
# TLS_KEY=/path/to/key.pem

# 禁用用户名密码登录，仅允许 OAuth2 登录 (可选)
# DISABLE_LOGIN=true